/* --- script_firebase.js --- */ async function initializeFirebase() { try { const config = JSON.parse(__firebase_config); if(!firebase.apps.length) firebase.initializeApp(config); const auth = firebase.auth(); db = firebase.firestore(); await auth.signInAnonymously(); currentUser = auth.currentUser; if (currentUser) { const path = `artifacts/${appId}/users/${currentUser.uid}/chatHistories/${canvasId}`; projectsCollectionRef = db.collection(`${path}/projects`); chatSessionsCollectionRef = db.collection(`${path}/sessions`); listenToProjects(); listenToChatSessions(); } } catch(e) { console.error("Firebase init failed:", e); } } function getRelativeDateGroup(ts, pinned=false) { if(pinned) return {key:0, label:"📌 고정됨"}; if(!ts) return {key:99, label:"날짜 정보 없음"}; const n=new Date(), d=ts.toDate();n.setHours(0,0,0,0);d.setHours(0,0,0,0);const diff=n.getTime()-d.getTime();const days=diff/(1000*60*60*24);if(days<1)return{key:1,label:"오늘"};if(days<2)return{key:2,label:"어제"};if(days<7)return{key:3,label:"지난 7일"};return{key:4,label:"이전 대화"};} function listenToProjects(){ if(!projectsCollectionRef)return; if(unsubscribeFromProjects)unsubscribeFromProjects(); unsubscribeFromProjects = projectsCollectionRef.onSnapshot(s=>{const old=localProjectsCache.slice(); localProjectsCache=s.docs.map(d=>({id:d.id, isExpanded:old.find(p=>p.id===d.id)?.isExpanded??true, ...d.data()})); renderSidebarContent(); if(newlyCreatedProjectId){startProjectRename(newlyCreatedProjectId); newlyCreatedProjectId=null;} });} function listenToChatSessions(){ if(!chatSessionsCollectionRef)return; if(unsubscribeFromChatSessions)unsubscribeFromChatSessions(); unsubscribeFromChatSessions = chatSessionsCollectionRef.onSnapshot(s=>{localChatSessionsCache = s.docs.map(d=>({id:d.id, ...d.data()})); renderSidebarContent(); if(currentSessionId){const data=localChatSessionsCache.find(s=>s.id===currentSessionId); if(!data)handleNewChat(); else renderChatMessages(data);} });}