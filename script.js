
document.addEventListener('DOMContentLoaded', function () {
    // All functions are now in separate files.
    // This script's only purpose is to initialize the application.
    initialize();
});
